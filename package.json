{
  "name": "election-assistance",
  "version": "1.0.0",
  "description": "NirvachakSetu Election Education Platform",
  "main": "backend/server.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "install-backend": "cd backend && npm install",
    "postinstall": "npm run install-backend"
  },
  "dependencies": {
    "@google/genai": "^1.50.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.5.0"
  }
}
